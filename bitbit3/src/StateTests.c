#include "StateTests.h"

#include <gb/gb.h>
#include <stdio.h>

#include "Vector.h"
#include "Stack.h"

void StartStateTests() {
	UINT8 i;

	/*DECLARE_VECTOR(v_test, 10);
	VectorAdd(v_test, 0u);
	VectorAdd(v_test, 3u);
	VectorAdd(v_test, 2u);
	VectorAdd(v_test, 1u);
	PRINT_VECTOR(v_test);

	VectorRemovePos(v_test, 2);
	PRINT_VECTOR(v_test);

	VectorRemovePos(v_test, 0);
	PRINT_VECTOR(v_test);

	VectorAdd(v_test, 5);
	PRINT_VECTOR(v_test);*/

	/*DECLARE_STACK(s_test, 10);
	StackPush(s_test, 0);
	StackPush(s_test, 3);
	StackPush(s_test, 2);
	StackPush(s_test, 5);
	PRINT_STACK(s_test);

	printf("Pop:%u\n", (UINT16)(StackPop(s_test)));
	PRINT_STACK(s_test);

	printf("Pop:%u\n", (UINT16)(StackPop(s_test)));
	PRINT_STACK(s_test);

	printf("Pop:%u\n", (UINT16)(StackPop(s_test)));
	PRINT_STACK(s_test);*/
}

void UpdateStateTests() {

}